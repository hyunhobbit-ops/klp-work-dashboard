# Phase 2 XSS 봉합 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Codex High 3건(#4 proposal-view 저장형 XSS, #5 URL 바로가기 위험 URL + 저장형 XSS, #6 doc-generator hash 반사형 XSS) 일괄 봉합.

**Architecture:** 3개 파일에 각각 동일한 `escHtml` 함수 추가, `app.js`에 `isSafeUrl` 추가. proposal-view의 사용자 입력 렌더 사이트에 escape 적용, URL 바로가기는 저장+렌더 양쪽 검증 + inline onclick 제거하고 data-* + 위임 리스너로 전환, doc-generator의 hash 출력 두 곳에 esc 적용.

**Tech Stack:** Vanilla JS (DOM API, addEventListener), 외부 의존성 추가 없음.

**Test 환경:** 자동 테스트 프레임워크 없음. 수동 브라우저 시나리오 G1~G6.

**스펙 참조:** `docs/superpowers/specs/2026-05-19-phase2-xss-design.md` (commit `3e533b8`)

---

## File Structure

| 파일 | 변경 |
|---|---|
| `proposal-view.html` | `escHtml` 신규 + `render()` 함수 내 사용자 입력 사이트 escape 적용 |
| `app.js` | `isSafeUrl` 신규 + `urlShortcutItemHtml` 리팩터 + `renderUrlShortcuts`에 위임 리스너 + `saveUrlShortcut` 검증 |
| `doc-generator.html` | `esc()` 강화(`"` 추가) + line 2033/2043 esc 적용 |

DB 변경 없음. SQL 마이그레이션 없음.

---

## Phase A — 코드 변경

### Task 1: proposal-view.html XSS escape (#4)

**Files:**
- Modify: `proposal-view.html` (line 75 근처 `<script>` 시작부 + 184~289 `render()` 내부)

- [ ] **Step 1: `escHtml` 함수 추가**

`proposal-view.html`의 `<script>` 블록 안, `const sb = window.supabase.createClient(...)` (line 79) 직후에 다음 추가:

```javascript
// XSS escape — &, <, >, " 4글자
function escHtml(s) {
    return String(s == null ? '' : s)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');
}
```

- [ ] **Step 2: `_optDisplayType` 결과를 escape하도록 호출부 수정**

`render()` 안에서 `_optDisplayType(o)` 호출은 line 204, 207에 있음(prints, packs map). 각 호출을 `escHtml(_optDisplayType(o))`로 교체:

```javascript
// 변경 전 (line 203~205)
const printRow = prints.length === 0 ? '' : prints.map(o =>
    `<div class="pp-opt-row"><span class="pp-opt-label">인쇄</span><span class="pp-opt-value"><span style="font-weight:600;color:#000">${_optDisplayType(o)}</span> <span style="color:#000;font-size:14px">${feeText(o)}</span></span></div>`
).join('');

// 변경 후
const printRow = prints.length === 0 ? '' : prints.map(o =>
    `<div class="pp-opt-row"><span class="pp-opt-label">인쇄</span><span class="pp-opt-value"><span style="font-weight:600;color:#000">${escHtml(_optDisplayType(o))}</span> <span style="color:#000;font-size:14px">${feeText(o)}</span></span></div>`
).join('');
```

같은 패턴으로 `packRow` (line 206~208) 도 적용.

- [ ] **Step 3: `labelRow`의 `o.note` escape**

line 209~211 의 `o.note` 직접 사용을 escape:

```javascript
// 변경 전
const labelRow = labels.length === 0 ? '' : labels.map(o =>
    `<div class="pp-opt-row"><span class="pp-opt-label">라벨</span><span class="pp-opt-value"><span style="font-weight:600;color:#000">부착 가능${o.note ? ' · ' + o.note : ''}</span> <span style="color:#000;font-size:14px">${feeText(o)}</span></span></div>`
).join('');

// 변경 후
const labelRow = labels.length === 0 ? '' : labels.map(o =>
    `<div class="pp-opt-row"><span class="pp-opt-label">라벨</span><span class="pp-opt-value"><span style="font-weight:600;color:#000">부착 가능${o.note ? ' · ' + escHtml(o.note) : ''}</span> <span style="color:#000;font-size:14px">${feeText(o)}</span></span></div>`
).join('');
```

- [ ] **Step 4: `imgHtml`의 `p.image`(URL) + `p.name`(alt) escape**

line 213~215 의 img 태그 변경:

```javascript
// 변경 전
const imgHtml = p.image
    ? `<img src="${p.image}" alt="${p.name}">`
    : `<svg width="48" height="48" ...>`;

// 변경 후
const imgHtml = p.image
    ? `<img src="${escHtml(p.image)}" alt="${escHtml(p.name || '')}">`
    : `<svg width="48" height="48" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>`;
```

- [ ] **Step 5: 카드 본문 — `p.name`, `p.description` escape**

line 234~244 의 카드 템플릿:

```javascript
// 변경 전
return `<div class="pp-card">
    <div class="pp-card-img">${imgHtml}${badgeHtml}</div>
    <div class="pp-card-body">
        <div class="pp-card-name">${p.name}</div>
        ${p.description ? `<div class="pp-card-desc">${p.description}</div>` : ''}
        <div class="pp-card-price">${priceLine}</div>
        ${bulkHtml}
        <div class="pp-card-opts">${printRow}${packRow}${labelRow}</div>
    </div>
</div>`;

// 변경 후
return `<div class="pp-card">
    <div class="pp-card-img">${imgHtml}${badgeHtml}</div>
    <div class="pp-card-body">
        <div class="pp-card-name">${escHtml(p.name)}</div>
        ${p.description ? `<div class="pp-card-desc">${escHtml(p.description).replace(/\n/g, '<br>')}</div>` : ''}
        <div class="pp-card-price">${priceLine}</div>
        ${bulkHtml}
        <div class="pp-card-opts">${printRow}${packRow}${labelRow}</div>
    </div>
</div>`;
```

(`priceLine`은 숫자 + 'VAT 포함/별도' 고정 문자열로만 구성되어 안전. `bulkHtml`은 숫자만 사용해 안전. `badgeHtml`은 line 195에서 빈 문자열로 고정되어 안전.)

- [ ] **Step 6: hero 블록 — `ep.title`, `ep.clientName`, `ep.clientContact`, `ep.description`, `ep.assignee*` escape**

line 246~277 의 hero/info-row 템플릿:

```javascript
// 변경 전
document.getElementById('pvRoot').innerHTML = `
    <div class="pp-wrap">
        <div class="pp-hero">
            <div class="pp-hero-circle3"></div>
            <div class="pp-hero-left">
                <div class="pp-hero-title">${ep.title || '제안서 제목'}</div>
                <div class="pp-hero-sub">${ep.clientName || '거래처'}${ep.clientContact ? ' · ' + ep.clientContact + ' 님' : ''}</div>
                <div class="pp-hero-date">작성일 ${(ep.createdAt || '').substring(0, 10).replace(/-/g, '.')}</div>
            </div>
            <div class="pp-hero-right">
                <img src="logo.png" alt="KLP KOREA" class="pp-hero-logo">
            </div>
        </div>

        <div class="pp-info-row">
            <div class="pp-info-card">
                <div class="pp-info-title">제안 안내</div>
                <div class="pp-info-desc">${(ep.description || '본 제안서는 아래 상품 구성을 기반으로 작성되었습니다.').replace(/\n/g, '<br>')}</div>
                <div style="margin-top:14px;font-size:13px;color:var(--gray-600);font-weight:700">${validHtml}</div>
            </div>
            <div class="pp-info-card">
                <div class="pp-info-title">담당자 정보</div>
                <div class="pp-mgr-row">
                    <div class="pp-mgr-name">${ep.assignee || 'KLP 담당자'}${_assigneeRoleTitle(ep.assignee) ? ` <span style="font-size:17px;font-weight:600;color:#000;margin-left:6px">${_assigneeRoleTitle(ep.assignee)}</span>` : ''}</div>
                </div>
                <div class="pp-mgr-contact">
                    <div>📧 ${ep.assigneeEmail || 'klpkorea@agift.kr'}</div>
                    <div>📱 ${ep.assigneePhone || '02-2103-5757'}</div>
                </div>
            </div>
        </div>
        ...`;

// 변경 후 (사용자 입력 사이트만 escape, 고정 문자열 / _assigneeRoleTitle 결과는 그대로)
document.getElementById('pvRoot').innerHTML = `
    <div class="pp-wrap">
        <div class="pp-hero">
            <div class="pp-hero-circle3"></div>
            <div class="pp-hero-left">
                <div class="pp-hero-title">${escHtml(ep.title || '제안서 제목')}</div>
                <div class="pp-hero-sub">${escHtml(ep.clientName || '거래처')}${ep.clientContact ? ' · ' + escHtml(ep.clientContact) + ' 님' : ''}</div>
                <div class="pp-hero-date">작성일 ${(ep.createdAt || '').substring(0, 10).replace(/-/g, '.')}</div>
            </div>
            <div class="pp-hero-right">
                <img src="logo.png" alt="KLP KOREA" class="pp-hero-logo">
            </div>
        </div>

        <div class="pp-info-row">
            <div class="pp-info-card">
                <div class="pp-info-title">제안 안내</div>
                <div class="pp-info-desc">${escHtml(ep.description || '본 제안서는 아래 상품 구성을 기반으로 작성되었습니다.').replace(/\n/g, '<br>')}</div>
                <div style="margin-top:14px;font-size:13px;color:var(--gray-600);font-weight:700">${validHtml}</div>
            </div>
            <div class="pp-info-card">
                <div class="pp-info-title">담당자 정보</div>
                <div class="pp-mgr-row">
                    <div class="pp-mgr-name">${escHtml(ep.assignee || 'KLP 담당자')}${_assigneeRoleTitle(ep.assignee) ? ` <span style="font-size:17px;font-weight:600;color:#000;margin-left:6px">${_assigneeRoleTitle(ep.assignee)}</span>` : ''}</div>
                </div>
                <div class="pp-mgr-contact">
                    <div>📧 ${escHtml(ep.assigneeEmail || 'klpkorea@agift.kr')}</div>
                    <div>📱 ${escHtml(ep.assigneePhone || '02-2103-5757')}</div>
                </div>
            </div>
        </div>
        ...`;
```

(`_assigneeRoleTitle(ep.assignee)` 결과는 hardcoded allowlist 매핑(`'대표' | '팀장' | '실장' | '대리' | ''`)이라 escape 불필요. `(ep.createdAt || '').substring(0, 10).replace(/-/g, '.')`은 날짜 문자열 슬라이스라 안전.)

- [ ] **Step 7: HTML 문법 점검 (시각 확인)**

브라우저 환경 없이 escape 확인. 파일을 텍스트로 다시 읽어 변경된 사이트가 `escHtml(...)`로 감싸졌는지, 닫는 괄호 짝이 맞는지 시각 확인.

- [ ] **Step 8: 커밋**

```bash
git add proposal-view.html
git commit -m "proposal-view.html: escHtml 추가 + 사용자 입력 렌더 사이트 전수 escape (#4)"
```

(Co-Authored-By 트레일러 포함)

---

### Task 2: app.js URL 바로가기 hardening (#5)

**Files:**
- Modify: `app.js` — `isSafeUrl` 신규 / `urlShortcutItemHtml` 리팩터 / `renderUrlShortcuts` 위임 리스너 / `saveUrlShortcut` 검증

- [ ] **Step 1: `isSafeUrl` 헬퍼 추가**

`app.js`의 `escHtml` 함수 정의(line 13370) 바로 다음에 추가:

```javascript
// URL 스키마 검증 — http/https/mailto/tel 또는 상대 경로만 허용
function isSafeUrl(url) {
    const s = String(url || '').trim();
    if (!s) return false;
    // 상대경로/앵커/쿼리는 허용 (scheme 없음)
    if (s.startsWith('/') || s.startsWith('#') || s.startsWith('?')) return true;
    const match = s.match(/^([a-zA-Z][a-zA-Z0-9+.-]*):/);
    if (!match) return true; // scheme 없음 → 상대경로로 간주
    return ['http', 'https', 'mailto', 'tel'].includes(match[1].toLowerCase());
}
```

- [ ] **Step 2: `urlShortcutItemHtml` 리팩터 — inline onclick 제거, data-* 사용**

`app.js` line 719~729 의 `urlShortcutItemHtml` 함수 전체를 다음으로 교체:

```javascript
function urlShortcutItemHtml(s) {
    const rawUrl = String(s.url || '');
    const safeUrl = isSafeUrl(rawUrl) ? rawUrl : '#';
    const urlAttr = escHtml(safeUrl);
    const titleAttr = escHtml(s.title || '');
    const titleText = escHtml(s.title || '');
    return `<div class="url-user-row">
        <a class="nav-item nav-sub-item nav-external" href="${urlAttr}" target="_blank" rel="noopener" data-url="${urlAttr}" data-title="${titleAttr}">
            <span>${titleText}</span>
            <svg class="external-icon" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6M15 3h6v6M10 14L21 3"/></svg>
        </a>
        <button class="url-edit-btn" onclick="event.stopPropagation();openUrlShortcutModal(${s.id})" title="편집">✏️</button>
    </div>`;
}
```

⚠️ `${s.id}` 는 DB 정수 ID이므로 escape 불필요(타입 안전). 편집 버튼의 inline onclick은 유지(고정 함수명 + 정수 인자만이라 안전).

- [ ] **Step 3: 위임 클릭 핸들러 추가**

`urlShortcutItemHtml` 함수 정의 직전에 다음 함수 추가:

```javascript
// nav-external 클릭 위임 — data-url / data-title 에서 안전하게 값 추출 후 copyLittlyLink 호출
function _urlShortcutClickHandler(ev) {
    const a = ev.target.closest('.nav-external');
    if (!a) return;
    const url = a.dataset.url || '';
    const title = a.dataset.title || '';
    if (typeof copyLittlyLink === 'function') copyLittlyLink(url, title);
}
```

- [ ] **Step 4: `renderUrlShortcuts` 함수 끝에 위임 리스너 1회 등록**

```bash
grep -n "function renderUrlShortcuts" app.js
```

함수의 마지막 `}` 직전에 다음 코드 추가 (idempotent 보장을 위해 플래그 사용):

```javascript
    // 위임 클릭 리스너 1회만 등록 (재렌더 시 중복 등록 방지)
    const _urlGroup = document.getElementById('urlShortcutsGroup');
    if (_urlGroup && !_urlGroup._urlClickHooked) {
        _urlGroup.addEventListener('click', _urlShortcutClickHandler);
        _urlGroup._urlClickHooked = true;
    }
```

⚠️ 컨테이너 ID `urlShortcutsGroup` 가 실제로 존재하는지 grep으로 한 번 확인. 다른 이름이면 그 이름 사용.

```bash
grep -n "urlShortcutsGroup" app.js | head -3
```

- [ ] **Step 5: `saveUrlShortcut`에 저장 시 URL 검증 추가**

```bash
grep -n "function saveUrlShortcut\|async function saveUrlShortcut" app.js
```

함수 본문 시작 부분에서 `payload`(또는 동등한 변수) 구성 직후, INSERT/UPDATE 호출 직전에 다음 한 줄 추가:

```javascript
    if (!isSafeUrl(payload.url)) {
        showToast('허용되지 않는 URL 형식입니다 (http/https/mailto/tel만 가능)');
        return;
    }
```

⚠️ payload 객체 이름이 다를 수 있음(`row`, `data` 등) — 실제 코드 확인 후 url 필드 참조.

- [ ] **Step 6: 문법 점검**

```bash
node --check app.js
```

- [ ] **Step 7: 커밋**

```bash
git add app.js
git commit -m "app.js: URL 바로가기 scheme 검증(isSafeUrl) + inline onclick 제거 → data-* 위임 리스너 (#5)"
```

---

### Task 3: doc-generator.html hash XSS 픽스 (#6)

**Files:**
- Modify: `doc-generator.html` line 400 (`esc` 함수) + line 2033 + line 2043

- [ ] **Step 1: `esc()` 함수에 `"` escape 추가**

```bash
grep -n "function esc" doc-generator.html
```

line 400 의 한 줄짜리 정의를 변경:

```javascript
// 변경 전
function esc(s){return String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}

// 변경 후
function esc(s){return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}
```

(추가로 `s||''` → `s==null?'':s` 변경 — 0/false 같은 값을 빈 문자열로 잘못 처리하던 minor 버그 동시 수정. 기존 호출부에 영향 없음.)

- [ ] **Step 2: line 2033 — view-DOCNUM 처리 "문서 없음" 분기 escape**

```bash
grep -n "문서를 찾을 수 없습니다" doc-generator.html
```

`applyViewHash` IIFE 안의 다음 줄을 찾는다 (line 2033):

```javascript
if(!res||res.length===0){document.body.innerHTML='<div style="padding:20px;color:#888;font-size:13px;text-align:center;font-family:sans-serif">문서를 찾을 수 없습니다: '+docNum+'</div>';return;}
```

`docNum`을 `esc(docNum)`으로 교체:

```javascript
if(!res||res.length===0){document.body.innerHTML='<div style="padding:20px;color:#888;font-size:13px;text-align:center;font-family:sans-serif">문서를 찾을 수 없습니다: '+esc(docNum)+'</div>';return;}
```

- [ ] **Step 3: line 2043 — view 로드 실패 catch에서 e.message escape**

같은 함수의 catch 블록 (line 2043):

```javascript
}catch(e){console.error('view load failed',e);document.body.innerHTML='<div style="padding:20px;color:#c00;font-size:13px;font-family:sans-serif">로드 실패: '+e.message+'</div>';}
```

`e.message`를 `esc(e.message)`로 교체:

```javascript
}catch(e){console.error('view load failed',e);document.body.innerHTML='<div style="padding:20px;color:#c00;font-size:13px;font-family:sans-serif">로드 실패: '+esc(e.message)+'</div>';}
```

- [ ] **Step 4: 시각 점검**

`doc-generator.html` 파일에서 다음을 grep으로 확인:
```bash
grep -n "innerHTML.*docNum\|innerHTML.*e\.message" doc-generator.html
```

기대: 0행 (모두 `esc()` 거쳐서 출력됨).

- [ ] **Step 5: 커밋**

```bash
git add doc-generator.html
git commit -m "doc-generator.html: esc() '\\\"' 추가 + view-DOCNUM 분기 hash XSS 봉합 (#6)"
```

---

## Phase B — 실행 런북

### Task 4: 푸시 + Vercel 자동 배포 + G1~G6 검증

- [ ] **Step 1: 푸시**

```bash
git push origin master
```

- [ ] **Step 2: Vercel 배포 완료 대기 (1~2분)**

Vercel 대시보드에서 최근 deployment **Ready** 확인.

- [ ] **Step 3: G1 — 정상 데이터 회귀 점검**

기존 사용 중인 proposals 중 정상 데이터 1건의 공유 링크 열기 → 텍스트가 평소처럼 표시되어야 함(escape 후에도 정상 데이터는 화면 변화 없음).

- [ ] **Step 4: G2 — proposal-view 저장형 XSS 차단 확인**

Supabase Dashboard → Table Editor → proposals → 임의 row → `description` 컬럼을 일시적으로 다음으로 변경:
```
<script>alert('xss')</script>
```

해당 제안서의 공유 링크 열기 → `<script>...</script>` 텍스트가 화면에 그대로 표시되고 alert 안 뜸. 확인 후 description 원복.

- [ ] **Step 5: G3 — URL 바로가기 저장 시 거부**

대시보드 → URL 바로가기 추가 모달 → URL에 다음 입력 후 저장:
```
javascript:alert(1)
```
expected: "허용되지 않는 URL 형식입니다 (http/https/mailto/tel만 가능)" 토스트, 저장 안 됨.

다음도 시도:
- `data:text/html,<script>alert(1)</script>` → 거부
- `https://example.com` → 정상 저장
- `mailto:test@example.com` → 정상 저장
- `tel:+82212345678` → 정상 저장

- [ ] **Step 6: G4 — DB의 기존 악성 URL 렌더 차단 (선택)**

Supabase Dashboard에서 url_shortcuts에 임시로 `url = 'javascript:alert(1)'` 행을 INSERT 후 사이드바 새로고침 → 해당 항목 클릭해도 alert 안 뜸 (href="#" 으로 치환됨). 확인 후 그 row 삭제.

- [ ] **Step 7: G5 — doc-generator hash 반사형 XSS 차단**

브라우저 주소창에:
```
https://klp-work-dashboard.vercel.app/doc-generator.html#view-<img src=x onerror=alert(1)>
```
열기. 기대: alert 안 뜨고 "문서를 찾을 수 없습니다: <img src=x onerror=alert(1)>" 텍스트만 표시.

- [ ] **Step 8: G6 — 정상 흐름 회귀**

- 기존 URL 바로가기 클릭 → littly 복사 토스트 정상
- doc-generator 정상 문서 link로 열기 → 평소처럼 미리보기 표시

- [ ] **Step 9: 모든 게이트 통과 시 완료 보고**

사장님에게 "G1~G6 모두 통과, Phase 2 완료" 보고.

---

## 자가 점검

**Spec coverage:**
- ✅ D1 (URL 스키마 4종) — Task 2 Step 1의 `isSafeUrl` 배열
- ✅ D2 (저장+렌더 양쪽 검증) — Task 2 Step 2(렌더), Step 5(저장)
- ✅ D3 (상대 경로 허용) — `isSafeUrl`의 `s.startsWith('/') || s.startsWith('#') || s.startsWith('?')` 분기
- ✅ D4 (파일별 동일 함수) — proposal-view (Task 1), app.js (기존), doc-generator (기존 esc 강화)
- ✅ D5 (escape 후 `\n→<br>`) — Task 1 Step 5의 description, Step 6의 ep.description
- ✅ D6 (inline onclick 제거) — Task 2 Step 2, 3, 4
- ✅ D7 (`esc()` 강화) — Task 3 Step 1
- ✅ Gate G1~G6 — Task 4 Step 3~8

**Placeholder scan:**
- Task 2 Step 4의 컨테이너 ID, Step 5의 payload 변수 이름 — implementer가 grep으로 확정. 이는 plan 작성 시점에 확정 불가한 정보. 받아들임.

**Type consistency:**
- `escHtml` 함수 시그니처 일관 (모든 사용 위치에서 `(s) => string`)
- `isSafeUrl` 함수 시그니처 일관 (`(url) => boolean`)
- `_urlShortcutClickHandler` 정의(Task 2 Step 3) ↔ 등록(Step 4) ↔ 호출 컨텍스트(closest('.nav-external')) 일관
- DOM 속성 이름 `data-url`, `data-title` — Step 2 정의 ↔ Step 3 읽기 일관
