# KLP KOREA 협업툴 — 제안서 시스템 클로드코드 요청 프롬프트

파트별로 나눠서 요청하세요. 각 파트 완성 후 확인하고 다음 파트를 진행하세요.

---

## 파트 1: 사이드바에 제안서 그룹 추가

```
사이드바에 "제안서" 메뉴 그룹을 추가해줘.

위치: "업무" 그룹과 "자료실" 그룹 사이에 넣어줘.

추가할 HTML (기존 nav-group 패턴과 동일하게):
- nav-group-label: "제안서"
- nav-item: "상품 DB" (data-tab="product-db")
  - 아이콘: 큐브/박스 모양 (viewBox 0 0 24 24)
- nav-item: "제안서 관리" (data-tab="proposals")
  - 아이콘: 문서+체크 모양

app.js의 pageTitles 객체에도 추가:
- 'product-db': '상품 DB'
- 'proposals': '제안서 관리'

index.html에 빈 탭 컨테이너 추가:
- <main class="content" id="tab-product-db"></main>
- <main class="content" id="tab-proposals"></main>

기존 코드 패턴(switchTab, nav-item 클릭 이벤트 등)을 그대로 따라줘.
새 파일 만들지 말고, 기존 index.html과 app.js에 추가해.
```

---

## 파트 2: 상품 DB 화면

```
사이드바의 "상품 DB" 탭 화면을 구현해줘.

이 화면은 제안서에 사용할 상품을 등록·관리하는 곳이야.
데이터는 우선 JS 배열(productsDB)에 저장해. 나중에 Supabase 테이블로 전환할 예정.

[상품 데이터 구조]
const productsDB = [];
// 각 상품 객체:
{
  id: Date.now(),
  name: '',           // 상품명
  description: '',    // 상품 설명/부가정보
  category: '',       // 카테고리: 시계/굿즈/생활용품/필기구/식품/기타
  image: '',          // 이미지 URL 또는 base64
  unitPrice: 0,       // 단가
  vatIncluded: true,  // VAT 포함 여부
  printType: '',      // 인쇄: 불가/레이저각인/실크인쇄/패드인쇄/기타
  printFee: 0,        // 인쇄비 (개당)
  packagingType: '',  // 포장: 기본박스/선물포장/전용케이스/전용보관함/기타
  packagingFee: 0,    // 포장비 (개당)
  labelAvailable: true, // 라벨부착 가능 여부
  status: '판매 중',  // 판매 중/품절/단종
  createdAt: new Date().toISOString()
}

[페이지 레이아웃]
1. 상단: 제목 "상품 DB" + 설명 + "상품 등록" 버튼 (btn-primary)
2. 요약 카드 3개: 전체 상품 / 판매 중(초록) / 품절·단종(빨강)
3. 필터: 카테고리 칩(전체/시계/굿즈/생활용품/필기구/식품) + 검색바
4. 테이블 (기존 data-table 클래스 사용):
   컬럼: 이미지(36px 썸네일) | 상품명+설명 | 카테고리(뱃지) | 단가(VAT포함) | 인쇄+인쇄비 | 포장+포장비 | 라벨 | 상태
   - 인쇄 컬럼: "불가"는 회색 텍스트, 가능하면 코랄 뱃지 + 아래에 "₩N,000/개"
   - 포장 컬럼: 초록 뱃지 + 아래에 포장비
   - 라벨: "가능"=초록뱃지, "불가"=빨간뱃지
   - 상태: "판매 중"=badge-green, "품절"=badge-gray, "단종"=badge-red

[상품 등록 모달]
기존 openModal 패턴 활용. "상품 등록" 버튼 클릭 시:
- 상품명 (text)
- 상품 설명 (text)
- 카테고리 (select)
- 단가 (number)
- VAT 포함 여부 (checkbox, 기본 체크)
- 인쇄 가능 여부 (select: 불가/레이저각인/실크인쇄/패드인쇄/기타)
- 인쇄비 (number, 인쇄 "불가" 선택 시 숨김)
- 포장 방식 (select: 기본박스/선물포장/전용케이스/전용보관함/기타)
- 포장비 (number)
- 라벨부착 (select: 가능/불가)
- 상태 (select: 판매 중/품절/단종)
- 이미지 URL (text, 나중에 Supabase Storage로 변경 예정)
- "상품 등록" 제출 버튼

[행 클릭 시]
기존 detail-overlay/detail-panel 패턴으로 오른쪽 슬라이드 상세 패널.
상세에서 수정/삭제 가능.

[샘플 데이터]
productsDB에 5~6개 샘플 상품 넣어줘:
- 미니클락 골드 (시계, ₩85,000, 레이저각인 ₩2,000, 선물포장 ₩1,500, 라벨 가능)
- 순금 1돈 감사패 대형 (굿즈, ₩180,000, 각인 가능 ₩5,000, 전용보관함 ₩60,000, 라벨 가능)
- 센트하우스 와인 디퓨저 (생활용품, ₩88,000, 인쇄 불가, 선물포장, 라벨 가능)
- 파카 51 코어 볼펜 (필기구, ₩121,000, 레이저각인 ₩3,000, 선물포장, 라벨 불가)
- 대통령 취임 기념시계 (시계, ₩250,000, 후면각인 ₩8,000, 전용케이스 ₩15,000, 라벨 가능, 품절)
- UM2 접지형 멀티 충전기 (생활용품, ₩69,000, 레이저각인 ₩2,000, 선물포장 ₩1,500, 라벨 가능)

함수 네이밍은 기존 패턴 따라: renderProductDB, openProductDBModal, addProduct, showProductDetail 등
```

---

## 파트 3: 제안서 관리 화면 (목록)

```
사이드바의 "제안서 관리" 탭 화면을 구현해줘.

데이터는 우선 JS 배열(proposals)에 저장.

[제안서 데이터 구조]
const proposals = [];
{
  id: Date.now(),
  title: '',           // 제안서 제목
  clientName: '',      // 거래처명
  clientContact: '',   // 거래처 담당자
  clientPhone: '',     // 거래처 연락처
  clientEmail: '',     // 거래처 이메일
  description: '',     // 제안 안내 문구
  validUntil: '',      // 유효기간 (날짜)
  assignee: '',        // 담당자 (김현호/이현주 등)
  status: '작성 중',   // 작성 중/발송 완료/계약 성사/미성사
  items: [],           // 담은 상품 [{productId, quantity, subtotal}]
  totalAmount: 0,      // 총 제안 금액
  sentDate: '',        // 발송일
  shareLink: '',       // 공유 링크 (생성 시 자동)
  createdAt: new Date().toISOString()
}

[페이지 레이아웃]
1. 상단: 제목 "제안서 관리" + 설명 + "새 제안서" 버튼
2. 요약 카드 4개: 전체 / 발송 완료(파란) / 계약 성사(초록) / 이번 달 매출
3. 필터: 상태 칩(전체/작성 중/발송 완료/계약 성사/미성사) + 검색바
4. 테이블:
   컬럼: 제안서명 | 거래처 | 제안 금액 | 상태(뱃지) | 발송일 | 담당 | 공유
   - 상태 뱃지: 작성 중=badge-orange, 발송 완료=badge-blue, 계약 성사=badge-green, 미성사=badge-gray
   - 공유 컬럼: 외부링크 아이콘 클릭 시 shareLink를 클립보드에 복사 + showToast('링크가 복사되었습니다')
   - 발송일: 미발송이면 "—" 표시

[행 클릭 시]
제안서 편집 화면으로 전환 (파트 4에서 구현)

[샘플 데이터]
2~3개 샘플:
- "순금1돈 감사패 제안" / 지플러스타워 / ₩2,400,000 / 계약 성사 / 4/10 / 김현호
- "러쉬 성수 커스텀시계" / 러쉬코리아 / ₩8,500,000 / 발송 완료 / 4/09 / 이현주
- "롯데월드 천관사복 굿즈" / 롯데월드 / ₩15,000,000 / 작성 중 / — / 김현호

함수: renderProposals, openProposalModal 등
```

---

## 파트 4: 제안서 작성/편집 화면

```
"새 제안서" 버튼 클릭 또는 기존 제안서 행 클릭 시 제안서 작성/편집 화면을 구현해줘.

이 화면은 별도 탭이 아니라, tab-proposals 영역 안에서 목록↔편집을 전환하는 방식으로 해줘.
(목록 숨기고 편집 폼 표시 → 저장/취소 시 다시 목록 표시)

[레이아웃]
상단: "새 제안서 작성" (또는 "제안서 편집") + "← 목록으로" 버튼 + "미리보기" 버튼 + "저장" 버튼

[2열 - 거래처 정보 + 제안서 정보]
왼쪽 카드 - 거래처 정보:
- 거래처명 (text, 필수)
- 담당자 (text)
- 연락처 (text)
- 이메일 (text)

오른쪽 카드 - 제안서 정보:
- 제안서 제목 (text, 필수)
- 유효기간 (date)
- 담당자 (select: 현재 로그인 사용자 기본값 + 직원 목록)
- 제안 안내 문구 (textarea)

[담은 상품 영역]
- 헤더: "담은 상품" + N개 선택 + "상품 DB에서 추가" 버튼
- "상품 DB에서 추가" 클릭 → 모달에 productsDB 상품 목록이 체크박스로 표시
  - 판매 중 상품만 표시
  - 이미 담은 상품은 체크된 상태 + 비활성
  - "선택 완료" 버튼으로 닫기
- 담은 상품 리스트 (각 행):
  - 썸네일(44px) | 상품명+설명+카테고리 | 단가(VAT포함/별도) | 수량 입력(number input) | 소계(단가×수량, 자동계산) | X 삭제버튼
  - 수량 변경 시 소계와 총합 자동 재계산
- 하단: 총 제안 금액 (자동 합계)

[발송 영역]
- "PDF 다운로드" 버튼 (일단 placeholder — showToast('PDF 기능 준비 중'))
- "링크 생성 및 공유" 버튼 → shareLink 생성 + 클립보드 복사 + showToast
  - 링크는 일단 "#proposal-preview-{id}" 형태로 생성 (나중에 실제 URL로 변경)

[저장 로직]
- 필수값(거래처명, 제안서 제목) 확인
- proposals 배열에 추가/수정
- items 배열: [{productId, quantity, subtotal}]
- totalAmount 자동 계산
- 저장 후 목록으로 전환 + showToast('제안서가 저장되었습니다')

함수: openProposalEditor, saveProposal, addProductToProposal, removeProductFromProposal, recalcProposalTotal 등
```

---

## 파트 5: 제안서 미리보기 (거래처 공유용 카탈로그)

```
제안서 편집 화면의 "미리보기" 버튼 클릭 시 카탈로그 미리보기를 구현해줘.

이 화면은 거래처에게 보여지는 외부 공유 페이지야.
일단은 모달이나 전체화면 오버레이로 구현하고, 나중에 독립 URL로 분리할 예정.

[프리미엄 다크 헤더]
- 배경: #0c0f1a
- 장식: position absolute로 원형 보더 2~3개 (rgba 투명도)
- 왼쪽: "PRODUCT PROPOSAL" 라벨(작은 밝은색) + 제안서 제목(큰 흰색) + 행사 정보(회색)
- 오른쪽: "KLP KOREA" (큰 흰색) + "We Make Better Goods" (작은 회색)

[2열 - 제안 안내 + 담당자 정보]
- 왼쪽: 제안 안내 문구 (유효기간은 빨간색 강조)
- 오른쪽: 담당자 아바타(이니셜) + 이름 + 직책 + 이메일 + 전화번호

[필터 바]
- 왼쪽: 필터 칩 — 전체(N개) / 인쇄 가능 / 선물포장 / 10만원 이하
  - 실제 필터링 동작 구현
- 오른쪽: 갤러리뷰(기본) / 테이블뷰 전환 아이콘 버튼

[상품 카드 그리드 — 3열]
각 카드:
- 상품 이미지 (height:180px, 이미지 없으면 placeholder 아이콘)
  - BEST 또는 NEW 뱃지 (position absolute, 우측 상단)
- 상품명 (14px, font-weight:500)
- 부가 설명 (11px, 회색)
- 가격: ₩88,000 (VAT 포함) ← 가격 큰 글씨 + 괄호 안에 VAT 작은 글씨
- 구분선 (border-top)
- 옵션 테이블 (라벨 52px + 값):
  - 인쇄 : 불가(회색텍스트) 또는 레이저각인(코랄뱃지 #FAECE7/#712B13)
  - 인쇄비 : 개당 ₩N,000 (인쇄 가능한 상품만 이 행 표시)
  - 포장 : 선물포장 가능(초록뱃지) 또는 기본박스(회색텍스트)
  - 포장비 : 개당 ₩N,000 (별도 포장비 있을 때만 이 행 표시)
  - 라벨 : 부착 가능(초록뱃지 #EAF3DE/#27500A) 또는 부착 불가(빨간뱃지 #FCEBEB/#791F1F)

[테이블뷰] (갤러리/테이블 전환 시)
- 테이블 형태로 같은 상품 정보 표시

[하단 CTA 바]
- 왼쪽: "관심 있는 상품이 있으신가요?" + "상품을 선택하시면 견적서를 바로 받아보실 수 있습니다"
- 오른쪽: "PDF 다운로드" + "견적 요청하기" 버튼 (파란색)

[푸터]
- KLP KOREA · 주소 · 전화 · 이메일 · 웹사이트

[구현 방식]
- proposal-preview-overlay라는 새 오버레이 div를 index.html에 추가
- position: fixed, inset: 0, z-index: 400, overflow-y: auto, background: var(--bg)
- 닫기 버튼(X)으로 오버레이 닫기
- 데이터는 현재 편집 중인 제안서의 items를 productsDB와 매칭해서 렌더링

함수: openProposalPreview, closeProposalPreview, renderPreviewProducts, filterPreviewProducts
```

---

## 파트 6: CLAUDE.md 업데이트

```
CLAUDE.md에 제안서 시스템 관련 내용을 추가해줘. 기존 내용은 유지하고 아래 내용을 적절한 위치에 추가:

## 제안서 시스템
- 사이드바 "제안서" 그룹: 상품 DB / 제안서 관리
- **상품 DB**: 제안서에 사용할 상품 등록·관리 (productsDB 배열, 추후 Supabase 전환)
- **제안서 관리**: 거래처별 제안서 목록·작성·편집·발송 이력 (proposals 배열, 추후 Supabase 전환)
- **제안서 흐름**: 상품 DB 등록 → 제안서 작성 시 DB에서 골라 담기 → 수량 입력 → 저장 → 링크 공유 또는 PDF
- **미리보기**: 거래처가 보는 카탈로그 화면 (프리미엄 다크 헤더 + 3열 상품 카드 그리드)
- 상품 카드 옵션 표기: 인쇄/인쇄비/포장/포장비/라벨을 라벨+값 형태로, 가격은 "₩N (VAT 포함)" 형태
- 데이터: 현재 JS 배열 → Supabase `products`, `proposals`, `proposal_items` 테이블로 전환 예정
```

---

## 요청 순서

1단계: 파트 1 (사이드바 + 빈 탭) — 5분
2단계: 파트 2 (상품 DB) — 가장 큰 파트
3단계: 파트 3 (제안서 목록)
4단계: 파트 4 (제안서 작성/편집)
5단계: 파트 5 (미리보기 카탈로그)
6단계: 파트 6 (CLAUDE.md 업데이트)

각 파트가 끝나면 브라우저에서 확인하고 다음 파트를 요청하세요.
문제가 있으면 "파트 N에서 OO 부분이 안 돼" 라고 말하면 됩니다.
